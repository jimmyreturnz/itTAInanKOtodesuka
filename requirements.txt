# Core
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0

# Audio decoding and mel extraction (torchaudio is tried first, librosa is the
# fallback and provides BPM detection either way)
librosa>=0.10.0
soundfile>=0.12.0

# Star ratings from the osu! API during packing
requests>=2.28.0

# Reads OSU_API_KEY from a .env file if you keep one
python-dotenv>=1.0.0

# Progress output
tqdm>=4.65.0
